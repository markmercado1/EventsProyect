server:
  port: 7075

spring:
  application:
    name: ms-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Toshibby/clasesAvance.git
          searchPaths:
            - config-data
          default-label: main

  security:
    user:
      name: root
      password: 123456
